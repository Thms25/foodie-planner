import Link from "next/link";
import Image from "next/image";

export default function RecipeCard({ recipe, onDeleteRecipe }) {
  return (
    <section className="h-screen p-24">
      <h1>Recipe Card incoming</h1>
    </section>
  );
}
